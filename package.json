{
  "name": "fasting-pwa",
  "version": "0.3.2",
  "private": true,
  "description": "Test tooling only. The app itself is a zero-dependency vanilla PWA with no build step - nothing here is shipped to the browser.",
  "scripts": {
    "test": "node test/offline.test.js && node test/storage.test.js && node test/timer.test.js && node test/history.test.js && node test/layout.test.js && node test/upgrade.test.js",
    "test:offline": "node test/offline.test.js",
    "test:storage": "node test/storage.test.js",
    "test:timer": "node test/timer.test.js",
    "test:upgrade": "node test/upgrade.test.js",
    "test:history": "node test/history.test.js",
    "test:layout": "node test/layout.test.js"
  },
  "devDependencies": {
    "puppeteer": "^24.0.0"
  }
}
